const searchBase = require('../searchBase.js');

class Search extends searchBase {
    constructor(array) {
        super(array);
    }

    sort(barrier) {
        for(let i = 0; i < barrier.length; i++) {

        }
    }
}